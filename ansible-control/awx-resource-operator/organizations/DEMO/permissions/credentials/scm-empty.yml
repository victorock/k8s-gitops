---
apiVersion: tower.ansible.com/v1alpha1
kind: Module
metadata:
  # yamllint disable-line rule:line-length
  name: "organization:demo:permission.credential:scm-empty:team:sysops:role:use"
  namespace: ansible-tower
spec:
  tower_auth_secret: "tower.victorock.io"
  name: "tower_role"
  parameters:
    organization: "DEMO"
    team: "sysops"
    role: "use"
    credential: "scm-empty"
---
apiVersion: tower.ansible.com/v1alpha1
kind: Module
metadata:
  name: "organization:demo:permission.credential:scm-empty:team:devops:role:use"
  namespace: ansible-tower
spec:
  tower_auth_secret: "tower.victorock.io"
  name: "tower_role"
  parameters:
    organization: "DEMO"
    team: "devops"
    role: "use"
    credential: "scm-empty"
---
apiVersion: tower.ansible.com/v1alpha1
kind: Module
metadata:
  name: "organization:demo:permission.credential:scm-empty:team:netops:role:use"
  namespace: ansible-tower
spec:
  tower_auth_secret: "tower.victorock.io"
  name: "tower_role"
  parameters:
    organization: "DEMO"
    team: "netops"
    role: "use"
    credential: "scm-empty"
---
apiVersion: tower.ansible.com/v1alpha1
kind: Module
metadata:
  name: "organization:demo:permission.credential:scm-empty:team:secops:role:use"
  namespace: ansible-tower
spec:
  tower_auth_secret: "tower.victorock.io"
  name: "tower_role"
  parameters:
    organization: "DEMO"
    team: "secops"
    role: "use"
    credential: "scm-empty"
